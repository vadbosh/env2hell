# env2hell

Ассистент выполняет обычную команду вроде `env` — и все ваши ключи и токены
печатаются прямо в сессию, открытым текстом. Прочитать их может любой, у кого
есть доступ к этой переписке. И она не исчезает: сессия сохраняется файлом на
диске, попадает в индексы памяти и в ночные сводки. Так ключи и утекают.

env2hell такие команды запрещает и даёт ассистенту безопасную замену. Работает
с Claude Code, Opencode и Codex — на Linux, macOS и Windows.

[English version](README.md)

## Зачем это нужно

Приложение падает с ошибкой авторизации. Разработчик пишет ассистенту:
«разберись, почему 401». Тот начинает с самого разумного — проверяет, задан ли
вообще ключ:

```bash
env | grep -i api
```

Проверка была правильная. Но `grep` отбирает **строки**, а строка здесь — это
`NAME=value`, поэтому вместе с ответом «да, задан» в сессию печатается и само
значение. Тем же кончаются `cat .env` при разборе настроек и `env` целиком,
когда сравнивают локальный запуск с CI.

Дальше эта строка живёт своей жизнью: сессия сохраняется файлом на диске
(Claude Code пишет `.jsonl`), инструменты памяти её индексируют, ночью
собираются сводки. После одной такой команды менять придётся все ключи на
машине.

Так и появился этот репозиторий: пять токенов (Z.ai, DeepSeek, OpenRouter,
Tavily и собственный токен доступа ассистента) напечатались полностью — в
сессии, где всего-то требовалось назвать версию модели.

Просьба «так не делай» здесь не работает. Инструкция — это рекомендация, а
нужен запрет.

## Из чего состоит

| Команда | Назначение |
|---|---|
| `secrets-guard` | Запрещает команду до запуска. Ассистенты вызывают его сами. |
| `secrets-redact` | Скрывает секрет, который команда уже напечатала, пока его не прочитала модель. |
| `safe-env` | Печатает окружение, скрывая значения секретов. Замена, которой пользуется модель. |

Вместе с ними в каждый ассистент ставится файл правила
(`rules/secrets-hygiene.md`) — чтобы модель знала про замену, а не боролась с
запретом.

### Что запрещено

```
env                     printenv                export -p
set                     declare                 history
env | grep KEY          rtk env                 ls && env
cat .env                head ~/.bashrc          cat /proc/1/environ
cat ~/.ssh/id_rsa       head ~/.aws/credentials
```

### Что по-прежнему работает

```
safe-env                        # все переменные, значения секретов скрыты
safe-env | grep MODEL           # фильтровать можно — значения уже скрыты
echo "$ANTHROPIC_MODEL"         # одна переменная по имени
printenv PATH                   # одна переменная, явно
env VAR=value some-command      # задаёт переменную команде, ничего не печатает
source env/bin/activate         # здесь `env` — имя каталога, а не команда
git commit -m "fix env parsing" # и здесь — просто слово в тексте
```

Две последние строки важнее, чем кажется. Проверка, которая не отличает команду
`env` от того же слова в имени каталога или в сообщении коммита, в первый же
день мешает обычной работе — и её выключают.

### Что скрывается уже в выводе

`secrets-guard` разбирает команду и поэтому не знает заранее, что она напечатает.
Программа, которой передали пароль, печатает этот пароль обратно:

```
$ croc send report.pdf
On the other computer, run:
  croc --relay relay.example:9009 --pass <REDACTED:32> quiet-otter-lamp
```

Значение заменил `secrets-redact` — уже после того, как `croc` его напечатал, но
до того, как результат прочитала модель. Что он скрывает:

```
--pass VALUE            password=VALUE          TOKEN: VALUE
--token VALUE           api_key=VALUE           Authorization: Bearer VALUE
ghp_… glpat-… AKIA…     eyJ….eyJ….              https://user:pw@host
```

Что он намеренно оставляет как есть:

```
md5sum   deadbeefdeadbeefdeadbeefdeadbeef  report.pdf
commit   feedfacefeedfacefeedfacefeedfacefeedface
```

В md5 ровно 32 символа — столько же было в том пароле от реле. По длине их не
различить, поэтому решает подпись рядом: строку из случайных символов скрывают
только тогда, когда что-то в той же строке называет её паролем, токеном, ключом
или секретом. Иначе под замену попали бы все контрольные суммы и хеши коммитов в
сессии.

## Установка

### Linux и macOS

```bash
git clone <этот-репозиторий> env2hell && cd env2hell
./install.sh                 # во все найденные ассистенты
./install.sh --dry-run       # показать, что будет сделано, ничего не менять
./install.sh --ide claude    # только в один ассистент
```

Нужен Python 3.8 или новее: файлы настроек ассистентов — это JSON.

### Windows

Ни Python, ни Git Bash не требуются: у `secrets-guard` есть версия на
PowerShell, и установщик написан на нём же.

```powershell
.\install.ps1
.\install.ps1 -DryRun
.\install.ps1 -Ide claude
```

Если файл не запускается, его не пускает политика выполнения PowerShell:

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1
```

**После установки перезапустите ассистента.** Хуки и плагины читаются один раз,
при запуске; до перезапуска ничего не проверяется.

## Как этим пользоваться

Руками запускать нечего. Ассистент вызывает `secrets-guard` перед каждой
командой, а при отказе получает сообщение о том, чем пользоваться вместо неё:

```
[secrets-guard] Blocked: bare env/printenv/export/set/declare leaks secrets to
the session. Use `safe-env` (values masked), or `echo "$VAR"` for one value.
```

`safe-env` пригоден и для ручной работы — когда окружение нужно посмотреть, но
не хочется оставлять его в журнале:

```
$ safe-env | grep -E 'TOKEN|MODEL'
ANTHROPIC_MODEL=glm-5.2
GITHUB_PERSONAL_ACCESS_TOKEN=<REDACTED:93>
TAVILY_API_KEY=<REDACTED:57>
```

Длина значения сохраняется: она позволяет различить два разных ключа и при этом
не даёт ничего, чем можно воспользоваться.

## Проверка

```bash
./tests/test_guard.sh          # 47 случаев против версии для POSIX
./tests/test_guard.sh --pwsh   # те же 47 против версии для PowerShell
```

Обе версии дают `passed 47, failed 0`.

## Удаление

```bash
./uninstall.sh                 # отключить во всех ассистентах, убрать обе команды
./uninstall.sh --dry-run
./uninstall.sh --keep-bin      # только отключить, команды оставить
```

Копии файлов настроек, сделанные при установке, не удаляются: в них лежит то,
как настройки выглядели раньше, и удалять их означало бы обессмыслить саму
привычку их делать.

## Куда именно встраивается

| Ассистент | Механизм |
|---|---|
| Claude Code | Хук `PreToolUse`, шаблон `Bash`, в `settings.json` |
| Claude Code | Хук `PostToolUse`, шаблон `Bash`, в `settings.json` — это `secrets-redact` |
| Codex | Хук `PreToolUse`, шаблон `^Bash$`, в `hooks.json` |
| Codex | Хук `PostToolUse`, шаблон `^Bash$` — `secrets-redact`, только предупреждение |
| Opencode | Запрещающие правила `permission.bash` **и** плагин `tool.execute.before` |
| Opencode | Плагин `tool.execute.after` — это `secrets-redact` |

Opencode требует обоих слоёв. `permission.bash` сравнивает начало строки
команды, поэтому сам по себе он никогда не увидит `env | grep X`, `rtk env` или
`a && env`. Настоящую проверку выполняет плагин, вызывая тот же
`secrets-guard`, — так правило остаётся в одном месте вместо двух, которые со
временем разойдутся.

`secrets-redact` попадает в Claude Code и в Opencode разными путями. Claude Code
заменяет результат через поле `hookSpecificOutput.updatedToolOutput`; Opencode
передаёт плагину изменяемый `output`, и туда же записывается изменённая строка.
Оба вызывают один и тот же CLI — плагин в режиме `--filter`.

**Заменить результат в Codex нельзя, и причина видна в его исходниках.**
Структура `PostToolUseOutcome` (`codex-rs/hooks/src/events/post_tool_use.rs`)
содержит `should_block`, `additional_contexts` и `feedback_message` — и ничего,
чем заменить вывод. Поэтому там хук делает единственное, что остаётся: сообщает
через `additionalContext`, что в журнале сессии теперь лежит секрет.

```
[secrets-redact] This output contains 1 credential-shaped value(s). Hooks in
this assistant cannot remove it, so it is already in the transcript. Do not
repeat it, do not echo the command that produced it, and tell the user the
value has to be rotated.
```

В предупреждении нет ни самого значения, ни команды, которая его напечатала:
и то и другое положило бы вторую копию в тот самый журнал, о котором
предупреждают. Утечку это не отменяет — оно превращает молчаливую утечку в
ротацию, а это разница между «узнать сейчас» и «не узнать никогда».

## Документация

- [Как это устроено](docs/design.ru.md) — два прохода проверки и назначение каждого
- [Установка подробно](docs/install.ru.md) — по ассистентам, Windows, разбор отказов
- [Что считается секретом](docs/patterns.ru.md) — список шаблонов и как его дополнить

## Границы применимости

- `secrets-guard` видит команду **до** запуска и поэтому не знает, что она
  напечатает. Этот случай закрывает `secrets-redact`, но только задним числом:
  команда уже выполнилась, а телеметрия ассистента отправляет исходный вывод.
  Модель читает вывод со скрытым значением — у поставщика записан исходный.
- `secrets-redact` опирается на подпись рядом со значением. Секрет без такой
  подписи, похожий на обычный текст — например, парольная фраза из трёх
  английских слов, — проходит нетронутым.
- Это фильтр, а не изолированная среда. Он повышает цену обычной случайности,
  но не защищает от того, кто намеренно достаёт значение.
- Ключ, уже попавший в окружение, там и останется. Ассистент делает снимок
  окружения при запуске, поэтому удаление переменной из `.bashrc` не убирает её
  из работающей сессии — подробнее в разделе
  [как это устроено](docs/design.ru.md).

## Лицензия

MIT, текст — в файле [LICENSE](LICENSE).
